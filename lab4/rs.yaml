apiVersion: apps/v1
kind: ReplicaSet

Metadata:
  name: nginx-rs
spec:
 replicas: 3
 selector:
   matchLabels:
     app: rs-web
 template:
   metadata:
     labels:
       app: rs-web
   spec:
     containers:
     - name: nginx
       image: nginx
       imagePullPolicy: IfNotPresent

